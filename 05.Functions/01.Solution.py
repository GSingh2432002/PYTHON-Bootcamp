# Write a function to calculate and return the square of a number.
# Parameters:- The variables that are defined when the function is declared are known as a parameter.
# Arguments:- The values that are declared within a function when the function is called are known as an argument.
def square_of_num(number):
    return number ** 2
res = square_of_num(8)
print(res)